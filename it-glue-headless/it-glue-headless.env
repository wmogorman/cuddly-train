# --- Login (first run uses these; afterwards a saved session is reused) ---
ITG_EMAIL=you@yourdomain.com
ITG_PASSWORD=SuperSecretPassword

# --- Target org/network(s) ---
ORG_NAME=City of Haslet             # exact org name in IT Glue
NETWORK_NAME=                        # optional; leave blank to apply to all networks in the org

# --- SNMPv3 (MD5 + AES) ---
SNMPV3_USER=netglue-md5aes
SNMPV3_AUTH_PROTO=MD5               # (don’t change)
SNMPV3_AUTH_PASS=ChangeMeAuth123!
SNMPV3_PRIV_PROTO=AES               # (don’t change)
SNMPV3_PRIV_PASS=ChangeMePriv123!
